# POVA-GAMEPAD

POVA-GAMEPAD 中期面板 ID 设计提案网站。

## 在线发布

1. 将本目录中的全部文件上传到 GitHub 仓库根目录。
2. 打开仓库 `Settings` > `Pages`。
3. 在 `Build and deployment` 中选择 `Deploy from a branch`。
4. 选择 `main` 分支和 `/ (root)`，然后保存。

网站为纯静态页面，不需要安装依赖或执行构建命令。

## 本地查看

直接打开 `index.html` 即可。